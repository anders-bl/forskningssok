# Ikke pyenv-håndhevet på denne maskinen (ingen pyenv installert) — denne fila er
# per nå informativ. Verdien speiler venv/ som faktisk finnes i dette repoet
# (bygget mot 3.14.7, med reportlab/sqlite-vec/fastapi/httpx/uvicorn alle
# fungerende — "unngå 3.14+" fra hus-malen stemmer ikke for DETTE repoet).
# Kjør alltid venv/bin/python direkte fremfor å stole på denne.

3.14.7
